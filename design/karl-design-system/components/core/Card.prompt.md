Generic bordered, rounded surface — the base for settings sections, contract panels, and message cards.

```jsx
<Card>
  <h3>Section title</h3>
  <p>Body content.</p>
</Card>
```

Set `padded={false}` when the child manages its own padding (e.g. a table).
