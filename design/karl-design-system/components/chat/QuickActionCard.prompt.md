Suggested-question tile shown above the chat composer, on the animated union-blue gradient header. Always place on a dark/accent background — it's translucent white, not a standalone card.

```jsx
<QuickActionCard onClick={() => ask('What does this agreement say about scheduling?')}>
  What does this agreement say about scheduling?
</QuickActionCard>
```
