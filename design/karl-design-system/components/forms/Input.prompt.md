Standard text input for forms — onboarding fields, settings, admin search.

```jsx
<Input placeholder="What's the Sunday premium for a courtesy clerk?" />
```

Focus state uses the amber ring the product applies globally (`input:focus { outline: 2px solid #F59E0B }`).
