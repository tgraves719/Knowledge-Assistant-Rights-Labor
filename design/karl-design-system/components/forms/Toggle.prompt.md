Pill switch for binary settings — dark mode, developer mode, notification opt-in.

```jsx
<Toggle checked={darkMode} onChange={setDarkMode} label="Dark mode" />
```

Track color is gold when on (matches the product's dark-mode toggle recoloring).
